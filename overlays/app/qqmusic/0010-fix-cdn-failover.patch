diff -ruN a/modules/349/35229.js b/modules/349/35229.js
--- a/modules/349/35229.js
+++ b/modules/349/35229.js
@@ -402,10 +402,23 @@
       if (this.audio.src && this.cdnUtil.sortedCdnList) {
         var _this$currentSong;
 
-        if (this.cdnUtil.checkIsAvailable((_this$currentSong = this.currentSong) === null || _this$currentSong === void 0 ? void 0 : _this$currentSong.mid)) {
+        // checkIsAvailable reports the opposite of its name: it is true once
+        // the song has burnt its retry budget. Tested unnegated, the retry
+        // below could only run after a budget that only the retry itself
+        // spends, so a load error skipped the song on the first try and the
+        // CDN fallback never ran at all. The skip after the branches was
+        // unconditional too, so it also skipped a second time.
+        if (!this.cdnUtil.checkIsAvailable((_this$currentSong = this.currentSong) === null || _this$currentSong === void 0 ? void 0 : _this$currentSong.mid)) {
           var _this$currentSong2;
 
           this.cdnUtil.addDisabledCdn((_this$currentSong2 = this.currentSong) === null || _this$currentSong2 === void 0 ? void 0 : _this$currentSong2.mid, this.audio.src);
+          const position = this.audio.currentTime;
+          const resume = () => {
+            this.audio.removeEventListener('loadedmetadata', resume);
+            this.audio.currentTime = position;
+            this.audio.play();
+          };
+          this.audio.addEventListener('loadedmetadata', resume);
           this.play({
             index: this.index
           });
@@ -414,8 +427,6 @@
           this.playNext();
         }
       }
-
-      this.playNext(false, 0);
     });
     this.audio = audio;
     document.body.appendChild(this.audio);
